.mask{
	position: fixed;
	z-index: 1000;
	top:0;
	left:0;
	bottom:0;
	right:0;
	background-color:rgba($black,0.4);
	&.mask-white{
		background-color:rgba($white,0.4);
		.mask-close{
			background-color:rgba($white,0.4);
			border-color:rgba($black,0.4);
			&:before{
				color:rgba($black,0.4);
			}
		}
	}
	@extend .flex-container;
	&.mask-transparent{
		background-color:transparent;
	}
	.mask_content{
		position: relative;
		max-height:100%;
		max-width:100%;
	}
	.mask_close{
		position: absolute;
		background-color:rgba($black,0.4);
		top:-$mask-close-size - 10;
		right:0;
		width:$mask-close-size;
		height:$mask-close-size;
		line-height:$mask-close-size - 1;
		border:1px solid rgba($white,0.8);
		border-radius:50%;
		text-align:center;
		@extend .icon-close;
		&:before{
			color:rgba($white,0.8);
			font-family: 'icomoon' !important;
			font-size:$mask-close-size / 2 ;
		}
	}
	>img{
		max-height:100%;
		max-width:100%;
	}
}